<h2>Edit <%=@category.name%></h2>
<%= form_for @category do |f| %>
  <%= render 'form', f:f %>
<% end %>

<%= button_to "Delete this category", category_path(@category), method: :delete, :class => "btn btn-danger" %>
  
