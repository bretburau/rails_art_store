<h2>All Categories</h2>
<% if can? :create, Category %>
  <%= button_to "Create a new Category", new_category_path, :method => :get, :class => "btn btn-info"%>
<% end %>
<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category) %></li>
  <% end %>
</ul>