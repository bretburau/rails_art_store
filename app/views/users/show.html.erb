<h2><%=@user.first_name %> <%=@user.last_name%></h2>
<p>Email: <%=@user.email%></p>
<% if logged_in? %>
    <%= button_to "Edit your profile", edit_user_path(@user), :method => :get, :class => "btn btn-info" unless @user.id != current_user.id %>
<% end %>