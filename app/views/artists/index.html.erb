<h2>Choose an artist you'd like to view</h2>

<% Artist.all.each do |artist| %>
  <% if artist.is_artist? %>
    <%= button_to artist.name, artist_path(artist), :class => "btn btn-defualt"%>
    <br>
  <% end %>
<% end %>