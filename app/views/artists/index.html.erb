<h2>Choose an artist you'd like to view</h2>

<% Artist.all.each do |artist| %>
  <% if artist.is_artist? %>
    <%= button_to artist.name, artist_path(artist), :method => :get, :class => "btn btn-secondary"%>
    <br>
  <% end %>
<% end %>