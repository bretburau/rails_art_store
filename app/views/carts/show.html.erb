<h2>Shopping Cart</h2>
<ul>
  <% current_user.current_cart.line_items.each do |item| %>
    <li><%=link_to item.piece.name, piece_path(item.piece) %> $<%= item.piece.price%> Qty: <%= item.quantity %></li>
  <% end %>
</ul>